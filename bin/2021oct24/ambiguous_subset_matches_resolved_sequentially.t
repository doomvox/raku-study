# Test file created outside of h2xs framework.
# Run this like so: `perl ambiguous_subset_matches_resolved_sequentially.t'
#   doom@kzsu.stanford.edu     2021/10/24 12:56:54

#########################

# change 'tests => 1' to 'tests => last_test_to_print';

use Test::More;

use 5.10.0;
use warnings;
use strict;
$|=1;
use Data::Dumper;

ok(1, "If we made it this far, we're ok. All modules are loaded.");

#########################



done_testing();
